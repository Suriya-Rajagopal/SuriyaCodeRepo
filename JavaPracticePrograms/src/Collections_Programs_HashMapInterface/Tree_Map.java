package Collections_Programs_HashMapInterface;

/************************
 POINTS ABOUT TREEMAP
 - TREEMAP (C) is implementation class for MAP(I).
 - Duplicate Keys are not allowed but duplicate values are allowed.
 - NULL Key is NOT allowed.
 - The elements will be retrieved on natural sorting order.
 - When Duplicate key is added with different values last used value will be
   overwritten
 - if Key is null and value has unique values then null pointer exception
   as it affects the natural sorting order
 */
public class Tree_Map {
}
